#!/usr/bin/python3
"This module is to return the number of keys in dictionary"""


def number_keys(a_dictionary):
    """This function returns the number of keys in adictionary
        Arg: a_dictionary: dictionary used to count the number of keys
        Returns: number of keys in the dictionary
    """
    return (len(a_dictionary))
